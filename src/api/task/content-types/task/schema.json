{
  "kind": "collectionType",
  "collectionName": "tasks",
  "info": {
    "singularName": "task",
    "pluralName": "tasks",
    "displayName": "task",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "description": {
      "type": "text"
    },
    "deadline": {
      "type": "datetime",
      "required": true
    },
    "taskStatus": {
      "type": "enumeration",
      "required": true,
      "default": "to_do",
      "enum": [
        "to_do",
        "in_progress",
        "completed",
        "cancelled"
      ]
    },
    "priority": {
      "type": "enumeration",
      "required": true,
      "default": "low",
      "enum": [
        "low",
        "medium",
        "high"
      ]
    },
    "project": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::project.project",
      "inversedBy": "tasks"
    }
  }
}
